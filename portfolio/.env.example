# ==================================
# CONFIGURATION FILE (.env)
# ==================================

# --- 1. POSTGRES (Standard) ---
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=admin
DB_PASS=secret

# --- 2. ORACLE (Enterprise) ---
# Oracle uses 'Service Name' or 'SID' instead of DB_NAME
ORACLE_HOST=oracle.corp.com
ORACLE_PORT=1521
ORACLE_SERVICE=ORCL_Prod
ORACLE_USER=system
ORACLE_PASS=oracle123

# --- 3. REDSHIFT (Data Warehouse) ---
# Redshift is basically Postgres on steroids (commands are same)
REDSHIFT_HOST=cluster.redshift.amazonaws.com
REDSHIFT_PORT=5439
REDSHIFT_DB=analytics
REDSHIFT_USER=awsuser
REDSHIFT_PASS=aws_secret

# --- 4. MONGODB (NoSQL) ---
# No "Host/Port" split usually. Just one URI.
MONGO_URI=mongodb://user:pass@cluster0.mongodb.net/my_collection
