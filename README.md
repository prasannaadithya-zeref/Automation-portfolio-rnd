# Data Validation & Automation Engineer Portfolio

A modern, responsive portfolio built with React, Tailwind CSS, Framer Motion, and shadcn/ui.
Designed for [Prasanna Adithya](https://github.com/prasannaadithya-zeref), showcasing skills in Python automation, data validation, and comparisons.

## 🚀 Tech Stack

- **Frontend:** React (Vite)
- **Styling:** Tailwind CSS
- **Components:** shadcn/ui (radix-ui)
- **Animations:** Framer Motion
- **Icons:** Lucide React

## 🛠️ Setup & Installation

Since this project was generated with a specific dependency version in mind, please follow these steps:

1.  **Install Node.js**: Ensure you have Node.js installed.
2.  **Install Dependencies**:
    ```bash
    npm install
    # or
    yarn install
    ```
3.  **Run Development Server**:
    ```bash
    npm run dev
    ```
4.  **Build for Production**:
    ```bash
    npm run build
    ```

## 📦 Deployment to GitHub Pages

1.  Initialize git if not already initialized:
    ```bash
    git init
    ```
2.  Add files:
    ```bash
    git add .
    git commit -m "Initial commit"
    ```
3.  Add your remote repository (replace with your URL):
    ```bash
    git remote add origin https://github.com/prasannaadithya-zeref/Automation-portfolio-rnd
    ```
4.  Push to main:
    ```bash
    git push -u origin main
    ```

## 🎨 Customization

- **Profile Data**: Edit `src/data/profile.ts` to update personal information, skills, and projects.
- **Styling**: Tailwind configuration is in `tailwind.config.js`.

## License

MIT
